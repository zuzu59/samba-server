#zf210525.1356
#source: https://stackoverflow.com/questions/39204142/docker-compose-with-multiple-databases

version: '3.3'

services:
    samba:
        image: dperson/samba:latest
        container_name: samba
        volumes:
            - $PWD/smb_share:/share
        restart: always
        # environment:
        #     SHARED_DIRECTORY: /nfsshare
        ports:
            - "139:139"
            - "445:445"
        # privileged: true
        command:
            -p
            -u "toto;tototutu."
            -s "toto2;/share;no;no;no;toto"
